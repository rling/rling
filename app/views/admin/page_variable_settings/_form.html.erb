<%= form_for(@page_variable_setting) do |f| %>
  <% if @page_variable_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_variable_setting.errors.count, "error") %> prohibited this page_variable_setting from being saved:</h2>

      <ul>
      <% @page_variable_setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :default_value %><br />
    <%= f.text_area :default_value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
