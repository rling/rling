<%= link_to 'New Page', new_page_path %> | <%= link_to 'Page Variable Settings', page_variable_settings_path %> | <%= link_to 'Back to Dashboard', admin_dashboard_path %><br />

<h1>Listing Pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Perma link</th>
    <th>Home page</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= page.perma_link %></td>
    <td> <%=  page.home_page ? "Yes" : "No" %></td>
    <td><%= link_to "Show", page %></td>
    <td><%= link_to "Edit", edit_page_path(page) %></td>
    <td><%= link_to "Destroy", page, :confirm => 'Are you sure?', :method => :delete %></td>
   
  </tr>
<% end %>
</table>

<br />
<%= link_to 'New Page', new_page_path %> | <%= link_to 'Page Variable Settings', page_variable_settings_path %> | <%= link_to 'Back to Dashboard', admin_dashboard_path %><br />

