<%= link_to image_tag('edit.png',:class=>'image_link'), edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>|<%= link_to 'Back to Dashboard',admin_dashboard_path %><br />

<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @page.title %>
</p>

<p>
  <b>Body:</b>
  <%= raw @page.body %>
</p>

<p>
  <b>Perma link:</b>
  <%= @page.perma_link %>
</p>

<p>
  <b>Home page:</b>
  <% if @page.home_page.eql?(false) %>
      <%= "No" %>
  <% else %>
       <% "Yes" %>
   <% end %> 
</p>

<p>
  <b>Page view type:</b>
  <% if @page.page_view_type.eql?(0) %>
     <%= "Computer and Mobile Browsers "%>
  <% elsif @page.page_view_type.eql?(1) %>
      <%= "Computer Browsers" %>
   <% else %>
<% Mobile Browsers %>
 <% end %>
 
</p>



<%= link_to image_tag('edit.png',:class=>'image_link'), edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>|<%= link_to 'Back to Dashboard',admin_dashboard_path %><br />
