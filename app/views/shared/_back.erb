<% if current_user? %>
  <% if current_user.admin? %>
   | <%= link_to 'Back to Dashboard', admin_dashboard_path %>
  <% else %>
   | <%= link_to 'Back', :controller=>"users",:action=>"show",:id=>current_user.id %>
  <% end %>
<% end %>