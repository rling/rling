  <style>
    #header_text{
      background: #<%= session[:settings][:header_background_color] %>;
    }
    #top_bar{
      background: #<%= session[:settings][:top_bar_background_color] %>;
    }
    #dashboard_main
    {
      background: #<%= session[:settings][:dashboard_main_background_color]%>;
    }
    
    #developer a
    {
      color: #<%=  session[:settings][:footer_link]%>;
    }  
    
    #header_text{
       font-family:<%= session[:settings][:header_text] %>;     
    }
    
    #top_bar
    {
       font-family:<%= session[:settings][:top_bar_text] %>;
    }
    
    #header_menu ul li a
    {
      color:#<%= session[:settings][:header_text_menu_color] %>; 
    }
    
    #header_menu ul li:hover
    {
       background:#<%=  session[:settings][:header_text_hover_color] %>;
      
    }
    
    #footer_menu ul li a:hover
    {
       color:#<%=  session[:settings][:footer_hover] %>;
    }
    
    #footer_menu ul
    {
        font-family:<%= session[:settings][:footer_text] %>;
    }
    #top_bar 
    {
      color:#<%= session[:settings][:top_bar_text_color] %>;
    }
   
    #top_bar
    {
      font-size: <%=session[:settings][:top_bar_font_size] %>px;
    }
    
    #heading
    {
      font-size:<%= session[:settings][:heading_font_size]  %>px;
    }
    
    #heading_tag{
      font-size:<%= session[:settings][:heading_tag_font_size] %>px;
    }
    
    #heading_tag
    {
      color:#<%= session[:settings][:heading_tag_color] %>;
    }
    
    #heading
    {
      color:#<%= session[:settings][:heading_color] %>;
    }

   #header_menu
   {
      background:#<%=  session[:settings][:header_menu_color] %>;
   }

   #middle
   {
      background:#<%=  session[:settings][:middle_color] %>;
   }  
   
   #sddm div a:hover
   {
     background:#<%= session[:settings][:submenu_hover] %>
   }    
    
   #sddm 
   {
     color:#<%= session[:settings][:submenu_text] %>
   }
   
   #heading_tag
   {
     font-family:#<%= session[:settings][:heading_tag_setting_value] %>
   }
   
   #heading
   {
     font-family:#<%= session[:settings][:heading_setting_value] %>
   }    
   
  
      
  </style>
