  <style>
    #header_text{
      background: #<%= session[:settings][:header_background_color] %>;
    }
    #top_bar{
      background: #<%= session[:settings][:top_bar_background_color] %>;
    }
    #dashboard_main
    {
      background: #<%= session[:settings][:dashboard_main_background_color]%>;
    }
    
    #developer a
    {
      color: #<%=  session[:settings][:footer_link]%>;
    }  
    
    
   
    #header_menu ul li a
    {
      color:#<%= session[:settings][:header_text_menu_color] %>; 
    }
    
    #header_menu ul li:hover
    {
       background:#<%=  session[:settings][:header_menu_hover_color] %>;
      
    }
    
    #header_menu ul li a:hover
    {
      color:#<%=  session[:settings][:header_text_hover_color] %>;
    }
   
    #top_bar 
    {
      color:#<%= session[:settings][:top_bar_text_color] %>;
    }
   
    
    #heading
    {
      font-size:<%= session[:settings][:heading_font_size]  %>px;
    }
    
    #heading_tag{
      font-size:<%= session[:settings][:heading_tag_font_size] %>px;
    }
    
    #heading_tag
    {
      color:#<%= session[:settings][:heading_tag_color] %>;
    }
    
    #heading
    {
      color:#<%= session[:settings][:heading_color] %>;
    }

   #header_menu
   {
      background:#<%=  session[:settings][:header_menu_color] %>;
   }

   #middle
   {
      background:#<%=  session[:settings][:middle_color] %>;
   }  
   
  
   #top_bar a
   {
     color:#<%= session[:settings][:top_bar_link] %>
   }
   
   #top_bar a:hover
   {
     color:#<%= session[:settings][:top_bar_link_hover] %>
   }
  
  #admin
  {
     background-color:#<%= session[:settings][:middle_background_color] %>
  }
  
  #admin
  {
     color:#<%= session[:settings][:middle_text_color] %>
  }
  
  #developer
  {
    color:#<%= session[:settings][:footer_text_color] %>
  }
  
 
  #developer a:hover
  {
    color:#<%= session[:settings][:footer_link_hover_color] %>
  }
  
  #footer
  {
    background-color:#<%= session[:settings][:footer_link_background_color] %>
  }
  
  body
  {
    background-color:#<%= session[:settings][:body_color] %>
  }
  
  #header_menu ul li
  {
    background-color:#<%= session[:settings][:header_menu_link_background] %>
  }
  
  </style>
