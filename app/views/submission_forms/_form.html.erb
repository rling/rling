<%= form_tag  ([@page,@submission_form]),:multipart=>true ,:method=>'put' do %>
  <% if @submission_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submission_form.errors.count, "error") %> prohibited this submission_form from being saved:</h2>

      <ul>
      <% @submission_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <% @submission_form.form_datas.each do |form_data| %>
    <p>
      <%= form_data.form_component.component_name %>:
      <%= edit_component_type(form_data) %>
      <%= form_data.form_component.mandatory ? "*" : "" %>
    </p>
    <% end %>
    <p> <%= submit_tag "Update" %> </p>

 <% end %>
